# 🤖 Два отдельных Telegram бота

Я разделил функциональность на **два независимых бота**!

## 📋 Что получилось

### 🎙️ Bot Voice (bot_voice.py)
- Распознает **голосовые сообщения**
- Работает с **аудиофайлами** (MP3, WAV, M4A)
- Выводит **транслит** (русский → латинский)
- **Отдельный токен!**

### 🌐 Bot Media (bot_media.py)
- Скачивает с **Instagram** (фото, видео, карусели)
- Скачивает с **TikTok** (видео)
- Скачивает с **Pinterest** (фото)
- **Отдельный токен!**

---

## 🚀 Как запустить

### Шаг 1: Получить 2 токена

🔴 **Первый токен для Bot Voice:**
1. Откройте Telegram
2. Напишите @BotFather `/newbot`
3. Назовите первого бота (например: `MyVoiceBot`)
4. Скопируйте первый токен

🔵 **Второй токен для Bot Media:**
1. Напишите @BotFather `/newbot` еще раз
2. Назовите второго бота (например: `MyMediaBot`)
3. Скопируйте второй токен

### Шаг 2: Вставить токены

**В файле bot_voice.py:**
```python
TOKEN_VOICE = "ВАШЕ_ЗНАЧЕНИЕ_ДЛЯ_ГОЛОСА"  # Измените это
```

**В файле bot_media.py:**
```python
TOKEN_MEDIA = "ВАШЕ_ЗНАЧЕНИЕ_ДЛЯ_МЕДИА"  # Измените это
```

### Шаг 3: Запустить оба бота

**Способ А - В разных терминалах (рекомендуется):**

Терминал 1:
```bash
python bot_voice.py
```

Терминал 2:
```bash
python bot_media.py
```

**Способ B - В одном терминале (фоновый запуск):**

Windows (PowerShell):
```powershell
Start-Process python bot_voice.py
Start-Process python bot_media.py
```

---

## 📊 Структура файлов

```
TELEGRAM_BOT/
├── bot.py              ← Оригинальный (комбинированный - можно удалить)
├── bot_voice.py        ← ⭐ БОТ 1: голос и аудио
├── bot_media.py        ← ⭐ БОТ 2: видео и фото
│
├── requirements.txt    ← Одинаково для всех
└── (документация)
```

---

## 🧪 Тестирование

### Проверить Bot Voice

1. Откройте Telegram
2. Найдите вашего **Voice Bot** (из первого токена)
3. Напишите `/start`
4. **Отправьте голосовое сообщение**
5. Бот должен вывести текст и транслит

### Проверить Bot Media

1. Откройте Telegram
2. Найдите вашего **Media Bot** (из второго токена)
3. Напишите `/start`
4. **Отправьте ссылку с Instagram/TikTok/Pinterest**
5. Бот должен скачать медиа

---

## 🎯 Использование в группах

### Voice Bot в группе
```
1. Откройте группу
2. Добавьте "Voice Bot" (Add members)
3. Отправьте голосовое сообщение
4. Bot выведет результат в чат
```

### Media Bot в группе
```
1. Откройте группу
2. Добавьте "Media Bot" (Add members)
3. Отправьте ссылку с Instagram/TikTok
4. Bot скачает и отправит медиа в чат
```

---

## 💡 Примеры использования

### Voice Bot
```
Пользователь: [отправляет голос]
Бот:
🎤 Голосовое сообщение распознано:

Оригинал:
Привет как дела?

Транслит:
Privet kak dela?
```

### Media Bot
```
Пользователь: https://instagram.com/p/ABC123/
Бот: ⏳ Обработка ссылки...
Бот: [отправляет фото/видео в чат]
```

---

## ⚙️ Настройка (опционально)

### Изменить язык распознавания (Voice Bot)

В `bot_voice.py` найдите:
```python
text = recognizer.recognize_google(audio_data, language='ru-RU')
```

Замените на:
```python
text = recognizer.recognize_google(audio_data, language='en-US')  # Английский
text = recognizer.recognize_google(audio_data, language='de-DE')  # Немецкий
```

### Изменить сообщение приветствия

**В bot_voice.py:**
```python
welcome_text = (
    "Моя кастомная приветствие!"
)
```

**В bot_media.py:**
```python
welcome_text = (
    "Моя кастомная приветствие!"
)
```

---

## 🐛 Устранение проблем

### Оба бота не запускаются
```bash
pip install -r requirements.txt
```

### "Invalid token"
- Убедитесь что скопировали токен полностью
- Проверьте что нет пробелов в начале/конце
- Разные токены для разных ботов!

### Voice Bot не распознает речь
- Проверьте интернет
- Говорите четче
- Уменьшите фоновый шум

### Media Bot не скачивает
- Проверьте что ссылка правильная
- Пост не должен быть приватным
- Обновите yt-dlp: `pip install --upgrade yt-dlp`

---

## 📝 Сравнение

| Функция | Voice Bot | Media Bot |
|---------|-----------|-----------|
| Голосовые сообщения | ✅ | ❌ |
| Аудиофайлы | ✅ | ❌ |
| Транслит | ✅ | ❌ |
| Instagram | ❌ | ✅ |
| TikTok | ❌ | ✅ |
| Pinterest | ❌ | ✅ |
| Независимые | ✅ | ✅ |
| Отдельные токены | ✅ | ✅ |

---

## 🚀 Продвинутые вещи

### Запустить оба бота одной командой (батник)

Создайте файл `run_both_bots.bat`:

```batch
@echo off
echo Запуск обоих ботов...
start cmd /k "python bot_voice.py"
start cmd /k "python bot_media.py"
echo Оба бота запущены!
pause
```

Двойной клик на файл - и оба бота запустятся!

### Использовать .env для токенов (безопаснее)

Установите:
```bash
pip install python-dotenv
```

Создайте файл `.env`:
```
TOKEN_VOICE=ваш_токен_голоса
TOKEN_MEDIA=ваш_токен_медиа
```

Обновите код:
```python
from dotenv import load_dotenv
import os

load_dotenv()
TOKEN_VOICE = os.getenv('TOKEN_VOICE')
TOKEN_MEDIA = os.getenv('TOKEN_MEDIA')
```

### Запустить на разных портах/машинах

Каждый бот полностью независим, поэтому:
- Bot Voice на машине 1
- Bot Media на машине 2
- Или оба на одной машине

---

## 📞 Быстрая справка

| Команда | Описание |
|---------|---------|
| `python bot_voice.py` | Запустить Voice Bot |
| `python bot_media.py` | Запустить Media Bot |
| `pip install -r requirements.txt` | Установить зависимости |
| `@BotFather /newbot` | Создать нового бота в Telegram |

---

## ✅ Чек-лист

- [ ] Получили **2 разных токена** от @BotFather
- [ ] Вставили токены в **bot_voice.py** и **bot_media.py**
- [ ] Установили зависимости: `pip install -r requirements.txt`
- [ ] Запустили оба бота в разных терминалах
- [ ] Проверили Voice Bot (голосовое сообщение)
- [ ] Проверили Media Bot (ссылка на Instagram/TikTok)
- [ ] Добавили ботов в группу и протестировали

**Готово! Оба бота работают независимо! 🎉**

---

**Примечание:** bot.py (оригинальный) теперь не нужен. Используйте bot_voice.py и bot_media.py!
