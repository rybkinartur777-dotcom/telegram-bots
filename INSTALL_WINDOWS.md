# 🤖 Telegram Bot - Транскрипция голосовых сообщений

## 📌 Быстрый старт (Windows)

### Шаг 1: Подготовка
1. **Скачайте и установите Python 3.8+** - https://www.python.org
   - При установке убедитесь что выбрали "Add Python to PATH"

2. **Скачайте и установите ffmpeg** - https://ffmpeg.org/download.html
   - Распакуйте архив (например, в `C:\ffmpeg`)
   - Добавьте папку `bin` в переменную окружения PATH:
     - Нажмите Win+X → System
     - Advanced system settings → Environment Variables
     - В "Path" нажмите Edit → New → добавьте `C:\ffmpeg\bin`
     - Нажмите OK и перезагрузитесь

### Шаг 2: Получение токена Telegram

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: `MyVoiceBot`)
   - Введите имя пользователя (например: `myvoicebot_bot`)
4. BotFather отправит вам токен вида:
   ```
   123456789:ABCDefGHIjkLmnoPQRstUvWxyzABCDefGH
   ```
5. **Скопируйте этот токен!**

### Шаг 3: Настройка бота

1. Откройте файл `bot.py` в текстовом редакторе (Notepad, VS Code и т.д.)
2. Найдите строку:
   ```python
   TOKEN = "8509159747:AAEj-w7cc5lh35hkHB1rTDNW-Gb139NVcqM"
   ```
3. Замените на ваш токен:
   ```python
   TOKEN = "ВАШ_ТОКЕН_ЗДЕСЬ"
   ```
4. Сохраните файл (Ctrl+S)

### Шаг 4: Запуск

**Способ A (Простой):**
- Дважды нажмите `install_and_run.bat`
- Скрипт сам установит все зависимости и запустит бота

**Способ B (Вручную):**
1. Откройте PowerShell в папке проекта
2. Выполните:
   ```powershell
   pip install -r requirements.txt
   python bot.py
   ```

## 🎯 Использование

### В личном чате с ботом:
1. Найдите вашего бота в Telegram
2. Пришлите `/start` для приветствия
3. Пришлите голосовое сообщение
4. Бот распознает речь и выведет:
   - **Оригинал** - текст на русском
   - **Транслит** - текст латинскими буквами

### В группе:
1. Добавьте бота в группу (нажмите на три точки → Add member)
2. Другие участники смогут отправлять голосовые сообщения
3. Бот будет отвечать в группу

## 📋 Что поддерживает бот

✅ Голосовые сообщения Telegram  
✅ Аудиофайлы (MP3, M4A, WAV и т.д.)  
✅ Русская речь  
✅ Автоматическая транслитерация в латиницу  
✅ Скачивание медиа с Instagram, TikTok, Pinterest (из оригинального кода)

## ❌ Если не работает

### "Python не найден"
- Переустановите Python и отметьте "Add Python to PATH"
- Перезагрузитесь

### "ffmpeg не найден"
- Скачайте ffmpeg с https://ffmpeg.org
- Добавьте его в PATH (см. выше)
- Перезагрузитесь

### "Ошибка при распознавании речи"
- Отправьте сообщение более четко
- Проверьте интернет-соединение
- Убедитесь что вы говорите на русском

### "Требуется доступ к интернету"
- Бот использует Google Speech Recognition API
- Нужно интернет соединение
- Проверьте брандмауэр/антивирус

## 🔧 Если хотите добавить свои функции

Отредактируйте `bot.py`:

```python
# Добавьте свой обработчик сообщений:
@dp.message()
async def my_handler(message: Message):
    if some_condition:
        await message.reply("Ваш ответ")
```

## 📚 Полезные ссылки

- Документация aiogram: https://docs.aiogram.dev/
- Speech Recognition: https://github.com/Uberi/speech_recognition
- Telegram Bot API: https://core.telegram.org/bots/api

## ❓ Вопросы?

Посмотрите логи в консоли при запуске - там будет информация об ошибках.

---

**Удачи! 🚀**
