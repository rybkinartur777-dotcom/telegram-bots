# ❓ FAQ - Часто задаваемые вопросы

## Общие вопросы

### Как бот узнает мой токен?
Используйте команду `/start` с BotFather в Telegram. Он отправит вам уникальный токен.

### Нужна ли база данных?
Нет. Бот работает без БД и обрабатывает каждое сообщение независимо.

### Могу ли я добавить бота в группу?
Да! Вот как:
1. Откройте группу
2. Нажмите на название группы (вверху)
3. Выберите "Add Members" 
4. Найдите вашего бота по имени или нажмите "Search"
5. Добавьте его

### Работает ли на English/иных языках?
Легко добавить. В `bot.py` найдите строку:
```python
text = recognizer.recognize_google(audio_data, language='ru-RU')
```

Замените `'ru-RU'` на нужный язык:
- `'en-US'` - English (USA)
- `'en-GB'` - English (UK)
- `'de-DE'` - Deutsch
- `'fr-FR'` - Français
- `'es-ES'` - Español

## Техничские вопросы

### Что такое токен?
Токен - это пароль для вашего бота. Храните его в секрете!

### Может ли кто-то украсть мой токен?
Да. Не выкладывайте его в открытом виде на GitHub.
Если украл - заново создайте бота у BotFather.

### Почему бот не отвечает?
1. Проверьте что бот запущен (в терминале будет "Бот запущен и готов!")
2. Убедитесь что используете правильный токен
3. Проверьте интернет
4. Перезапустите бота (Ctrl+C, потом заново запустите)

### Почему не распознает русский?
- Проговорите четче
- Уменьшите шум фонового
- Убедитесь что мicrophone работает
- Проверьте интернет (нужен для Google Speech API)

### Как отправить файл в группу от имени бота?
```python
# В handle_message или любом обработчике:
await message.answer_document(document=FSInputFile("path/to/file.txt"))
```

### Как изменить ответ бота?
Отредактируйте текст в `bot.py`:
```python
result_text = (
    f"<b>🎤 ВАШ ТЕКСТ:</b>\n\n"
    f"<b>Оригинал:</b>\n{text}\n\n"
    f"<b>Транслит:</b>\n{transliterated}"
)
```

## Ошибки

### "No such module 'pydub'"
```bash
pip install pydub
```

### "Unsupported audio file type"
Убедитесь что установлены ffmpeg и python-audio:
```bash
pip install --upgrade pydub
# и установите ffmpeg (смотри INSTALL_WINDOWS.md)
```

### "Connection error"
- Проверьте интернет
- Проверьте брандмауэр
- Google Speech API может быть недоступен - попробуйте VPN

### "Invalid token"
- Убедитесь что скопировали токен полностью
- Нет пробелов в начале/конце?
- Правильный символ ':' посередине?

### "Request timed out"
- Проверьте скорость интернета
- Попробуйте позже
- Аудиофайл может быть слишком большим

## Оптимизация

### Как ускорить распознавание?
1. Уменьшите длину аудиosообщения
2. Убедитесь что ffmpeg установлен (ускоряет конвертацию)

### Как сэкономить интернет?
Используйте локальное распознавание речи (требует MLKit):
```python
# Вместо Google Speech Recognition используйте offline версию
# Требует 500+ МБ моделей на английском
```

### Сколько сообщений может обработать?
Теоретически неограниченно, но:
- Один сервер обычно = 100+ одновременных пользователей
- Нужна более мощная машина для масштабирования

## Продвинутое

### Как запустить в облаке?
1. Загрузите на GitHub
2. Используйте PythonAnywhere, Heroku, или другой хостинг
3. Используйте Webhook вместо polling (в aiogram есть примеры)

### Как добавить базу данных?
```python
# Установите
pip install sqlalchemy

# И используйте в обработчике
from sqlalchemy import create_engine
engine = create_engine('sqlite:///bot.db')
```

### Как добавить логирование?
```python
import logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# В обработчике:
logger.info(f"Получено сообщение: {message.text}")
```

## Полезные команды

```bash
# Проверить версию Python
python --version

# Проверить установленные пакеты
pip list

# Обновить pip
python -m pip install --upgrade pip

# Установить спецификую версию пакета
pip install aiogram==3.0.0

# Удалить пакет
pip uninstall pydub

# Создать отдельное окружение (рекомендуется)
python -m venv venv
venv\Scripts\activate  # Windows
```

---

Если не нашли ответ - посмотрите логи в консоли! 📋
