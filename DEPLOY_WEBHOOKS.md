# 🚀 Деплой с Webhooks на Render

## ✅ Что изменилось

Боты переделаны для работы через **webhooks** вместо polling. Теперь они работают на **Web Service** Render!

## 📋 Инструкция по настройке

### Шаг 1: Обновить код на GitHub

```bash
cd C:\Users\rybki\OneDrive\Desktop\TELEGRAM_BOT
git add .
git commit -m "Update bots to use webhooks"
git push origin main
```

### Шаг 2: Настроить Environment Variables на Render

Для **telegram-media-bot**:

1. Открой Render Dashboard → telegram-media-bot → "Environment"
2. Добавь переменную:
   - **Key:** `RENDER_EXTERNAL_URL`
   - **Value:** (скопируй URL твоего сервиса, например `https://telegram-media-bot.onrender.com`)

Render автоматически установит переменную `PORT` - трогать её не нужно!

### Шаг 3: Ждём автоматического деплоя

После push на GitHub Render автоматически:
1. Скачает новый код
2. Установит зависимости (включая aiohttp)
3. Запустит бота с webhooks
4. Бот установит webhook URL в Telegram API

### Шаг 4: Проверка

1. В Render Dashboard открой "Logs" для telegram-media-bot  
2. Должна появиться строка: `[MEDIA BOT] Webhook set to: https://...`
3. Проверь бота в Telegram - отправь ссылку на TikTok/Instagram

## 🎉 Готово!

Теперь бот работает через webhooks и отвечает моментально!

---

## ℹ️ Как это работает

**Раньше (polling):**
- Бот постоянно опрашивал Telegram API: "Есть новые сообщения?"  
- На Web Service это не работало

**Теперь (webhooks):**
- Бот запускает веб-сервер на порту (Render дает PORT автоматически)
- Telegram отправляет обновления напрямую на URL бота
- Бот мгновенно обрабатывает сообщения

---

## Voice Bot

Voice Bot требует дополнительных системных зависимостей (ffmpeg, build-essential).  
На бесплатном Render это сложно. Рекомендации:

1. **Упростить Voice Bot** - убрать deepmultilingualpunctuation и whisper
2. **Использовать другой хостинг** для Voice Bot (Railway, Fly.io)
3. **Платный план Render** ($7/месяц) с полным доступом к системным пакетам

Сначала проверим работу Media Bot!
