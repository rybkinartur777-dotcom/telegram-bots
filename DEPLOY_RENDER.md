# 🚀 Деплой Telegram ботов на Render

## ✅ Что уже сделано

Твои боты подготовлены к деплою! Все токены теперь в переменных окружения.

## 📋 Шаги для деплоя на Render (бесплатно, работает 24/7)

### 1️⃣ Создай GitHub репозиторий

1. Зайди на https://github.com и создай новый репозиторий (например `telegram-bots`)
2. НЕ добавляй README, .gitignore - у нас уже есть

### 2️⃣ Загрузи код на GitHub

Открой терминал в папке `TELEGRAM_BOT` и выполни:

```bash
git init
git add .
git commit -m "Initial commit - telegram bots"
git branch -M main
git remote add origin https://github.com/ТвойUsername/telegram-bots.git
git push -u origin main
```

> **ВАЖНО:** Файл `.env` НЕ загрузится на GitHub (он в `.gitignore`) - это ПРАВИЛЬНО! Токены должны быть в секрете.

### 3️⃣ Создай два Web Service на Render

Перейди на https://render.com и зарегистрируйся (можно через GitHub).

#### Для Media Bot:

1. Нажми **"New +"** → **"Web Service"**
2. Подключи свой GitHub репозиторий `telegram-bots`
3. Заполни:
   - **Name:** `telegram-media-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot_media.py`
   - **Plan:** `Free`
4. В разделе **"Environment Variables"** добавь:
   - Key: `TOKEN_MEDIA`
   - Value: `8509159747:AAEj-w7cc5lh35hkHB1rTDNW-Gb139NVcqM`
5. Нажми **"Create Web Service"**

#### Для Voice Bot:

1. Снова нажми **"New +"** → **"Web Service"**
2. Подключи тот же репозиторий `telegram-bots`
3. Заполни:
   - **Name:** `telegram-voice-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt && apt-get update && apt-get install -y ffmpeg`
   - **Start Command:** `python bot_voice.py`
   - **Plan:** `Free`
4. В разделе **"Environment Variables"** добавь:
   - Key: `TOKEN_VOICE`
   - Value: `8292615120:AAHd0nIjycDuOlFN2Ro6dLXQWI_6fSiP560`
5. Нажми **"Create Web Service"**

### 4️⃣ Проверь статус

После создания Render начнет деплой:
- Открой каждый сервис
- Смотри на вкладку **"Logs"**
- Дождись сообщения `[MEDIA BOT] Started and ready for links!` или аналогичного

### 5️⃣ Проверь работу ботов

1. Открой Telegram
2. Найди своих ботов по username
3. Отправь команду `/start`
4. Проверь функционал:
   - **Media Bot:** отправь ссылку на Instagram/TikTok
   - **Voice Bot:** отправь голосовое сообщение

## 🎉 Готово!

Теперь боты работают **автономно 24/7** без твоего компьютера!

---

## 🔧 Альтернативные хостинги (если Render не подойдет)

### Railway.app
- Бесплатный план: 500 часов/месяц
- Очень простой деплой
- Автоматическая установка зависимостей

### Fly.io
- Бесплатно: 3 маленьких VM
- Поддержка Docker
- Хорошая документация

### PythonAnywhere (только для Python)
- Бесплатный план с ограничениями
- Простой веб-интерфейс
- Подходит для простых ботов

---

## 📝 Частые проблемы

### Бот не отвечает
- Проверь логи на Render (вкладка "Logs")
- Убедись что токены правильно добавлены в Environment Variables
- Проверь что сервис запущен (статус должен быть "Live")

### Voice Bot не работает
- Убедись что в Build Command добавлена установка ffmpeg:
  ```
  pip install -r requirements.txt && apt-get update && apt-get install -y ffmpeg
  ```

### Render засыпает (free tier)
- Бесплатный план Render засыпает после 15 минут неактивности
- Решение: используй сервис вроде UptimeRobot для пинга каждые 5 минут
- Или перейди на платный план ($7/месяц)

---

## 💰 Стоимость

- **Render Free:** Полностью бесплатно, но засыпает при неактивности
- **Render Paid:** $7/месяц за каждый сервис (работает всегда)
- **Railway:** $5/месяц за 500 часов

---

## 🆘 Нужна помощь?

Если что-то не получается - пиши! Помогу разобраться.
