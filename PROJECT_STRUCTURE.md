# 📁 Структура проекта

```
TELEGRAM_BOT/
├── bot.py                     # Главный файл бота
├── requirements.txt           # Зависимости Python
├── install_and_run.bat       # Скрипт быстрого запуска (Windows)
│
├── README.md                 # Основная документация
├── QUICKSTART.md             # Быстрый старт (5 минут)
├── INSTALL_WINDOWS.md        # Подробная установка для Windows
├── FAQ.md                    # Часто задаваемые вопросы
├── EXAMPLES.md               # Примеры кода для расширения
└── PROJECT_STRUCTURE.md      # Этот файл
```

## 📄 Описание файлов

### 🔴 bot.py
**Это самый важный файл!**
- Главный код бота
- Обработчики сообщений
- Функция транслитерации
- Запуск бота

**Что нужно изменить:**
```python
TOKEN = "ВАШЕ_ЗНАЧЕНИЕ"  # Замените на ваш токен от BotFather
```

### 📦 requirements.txt
Список всех Python библиотек которые нужны. Установка:
```bash
pip install -r requirements.txt
```

**Какие библиотеки:**
- `aiogram` - работа с Telegram Bot API
- `SpeechRecognition` - распознавание речи (Google API)
- `pydub` - преобразование аудиофайлов
- `yt-dlp` - скачивание видео (из оригинального кода)

### 🚀 install_and_run.bat
Скрипт для Windows. Запускает и устанавливает все автоматически:
```powershell
double-click install_and_run.bat
```

### 📖 Файлы документации

| Файл | Когда читать |
|------|-------------|
| README.md | Общее описание, все функции |
| QUICKSTART.md | Хочу быстро запустить (5 минут) |
| INSTALL_WINDOWS.md | Проблемы с установкой |
| FAQ.md | Вопросы по использованию |
| EXAMPLES.md | Хочу добавить свои функции |
| PROJECT_STRUCTURE.md | Инфо о структуре проекта |

## 🎯 Рекомендуемый порядок действий

### Для новичков:
1. Прочитайте **QUICKSTART.md** (2 мин)
2. Следуйте инструкциям из **INSTALL_WINDOWS.md** (10 мин)
3. Запустите `install_and_run.bat`
4. Тестируйте в Telegram

### Если что-то не работает:
1. Посмотрите **FAQ.md**
2. Проверьте вывод ошибок в консоли
3. Прочитайте **INSTALL_WINDOWS.md** раздел "Устранение проблем"

### Если хотите расширить:
1. Прочитайте **EXAMPLES.md**
2. Скопируйте пример кода в `bot.py`
3. Модифицируйте под себя
4. Перезапустите бота

## 🔧 Как структурирован bot.py

```python
# 1. Импорты - все необходимые библиотеки
import asyncio
from aiogram import Bot, Dispatcher
...

# 2. Конфигурация - токен и настройки
TOKEN = "ВАШ_ТОКЕН"
MAX_VIDEO_SIZE_BYTES = 50 * 1024 * 1024

# 3. Таблица транслитерации - русский → латинский
TRANSLIT_DICT = {
    'а': 'a',
    'б': 'b',
    ...
}

# 4. Функции-помощники
def transliterate(text):
    """Переводит кириллицу в латиницу"""
    ...

# 5. Инициализация бота
bot = Bot(token=TOKEN, ...)
dp = Dispatcher()

# 6. Обработчики сообщений - реагируют на события
@dp.message(CommandStart())
async def cmd_start(message: Message):
    ...

@dp.message()
async def handle_voice(message: Message):
    ...

# 7. Главная функция - запускает бота
async def main():
    print("Бот запущен и готов!")
    await dp.start_polling(bot)

# 8. Точка входа
if __name__ == "__main__":
    asyncio.run(main())
```

## 🎙️ Как работает обработка голоса

```
1. Пользователь отправляет голосовое сообщение
            ↓
2. Бот получает OGG файл от Telegram
            ↓
3. Конвертирует OGG → WAV (pydub + ffmpeg)
            ↓
4. Отправляет WAV в Google Speech Recognition API
            ↓
5. Получает текст на русском
            ↓
6. Преобразует в транслит (функция transliterate)
            ↓
7. Отправляет результат пользователю
```

## 📝 Что можно добавить

- Сохранение истории сообщений в БД
- Многоязычная поддержка
- Команды администратора
- Статистика использования
- Кнопки для выбора опций
- Inline режим (поиск)
- Уведомления
- Получение информации о боте

## ⚙️ Изменение функциональности

### Хочу другой язык?
В `bot.py` найдите:
```python
text = recognizer.recognize_google(audio_data, language='ru-RU')
```

Измените на нужный:
```python
text = recognizer.recognize_google(audio_data, language='en-US')  # Английский
text = recognizer.recognize_google(audio_data, language='de-DE')  # Немецкий
```

### Хочу отключить скачивание медиа?
Удалите или закомментируйте функцию `handle_message`:
```python
# @dp.message()
# async def handle_message(message: Message):
#     ...
```

### Хочу добавить нечто новое?
1. Скопируйте пример из **EXAMPLES.md**
2. Вставьте перед последним `@dp.message()` обработчиком
3. Модифицируйте под себя
4. Перезапустите бота

## 🚀 Развертывание на сервер

Если хочешь запустить 24/7:

1. **Локальный сервер (VPS):**
   - Скопируйте папку на VPS
   - Запустите `python bot.py`
   - Используйте `screen` или `tmux` для фонового запуска

2. **Облачный сервис:**
   - PythonAnywhere
   - Heroku (бесплатно с ограничениями)
   - AWS
   - Использует webhook вместо polling

3. **Docker:**
   - Создайте Dockerfile
   - Запустите контейнер
   - Масштабируйте по мере необходимости

## 📊 Производительность

- **Одно сообщение:** 2-5 секунд (зависит от сети и Google API)
- **Одновременные пользователи:** ~50-100 на одном сервере
- **Ограничения Telegram API:** 30+ сообщений в секунду

## 🔐 Безопасность

⚠️ **ВАЖНО:**
- Никогда не выкладывайте `TOKEN` на GitHub
- Используйте `.env` файл для переменных окружения
- Проверяйте rights пользователей для admin команд
- Логируйте подозрительную активность

Пример с `.env`:
```python
from dotenv import load_dotenv
import os

load_dotenv()
TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
```

## 📞 Поддержка

- Документация: https://docs.aiogram.dev/
- Telegram Bot API: https://core.telegram.org/bots/api
- Python aiogram: https://github.com/aiogram/aiogram

---

**Удачи в разработке! Если что-то непонятно - смотри FAQ.md или EXAMPLES.md** 🚀
